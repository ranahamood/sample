/*
 * Learning Messages
 *
 * This contains all the text for the Learning container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Learning';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Learning container!',
  },
});
