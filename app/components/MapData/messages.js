/*
 * MapData Messages
 *
 * This contains all the text for the MapData component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MapData';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MapData component!',
  },
});
